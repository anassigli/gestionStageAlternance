{% block body %}
    <div class="card">

        <div>
            <img src="{{ asset(img_enterprises ~ offer.enterprise.imageName) }}" class="w-1/4 float-right"
                 alt="logo de l'entreprise">
            <h2 class="text-xl font-semibold text-gray-800 mb-4"> {{ offer.name }}</h2>
            <div class="mb-4">
                <div class="pill-holder">
                    {% for tag in offer.tags %}
                        <span class="pill">{{ tag.tag }}</span>
                    {% endfor %}
                </div>
                <span class="text-sm font-medium text-gray-500">
                    Il y a {{ offer.candidacies|length }} candidature(s) pour cette offre.
                </span>
            </div>
            <div class="mb-4">
                <p class="text-style-3lines">
                    {{ offer.description }}
                </p>
            </div>
        </div>

        <div class="link-container">
            <div class="link-holder">
                <div><a href="{{ path('app_offers_show', {'id': offer.id}) }}"
                        class="rounded-md p-2 text-white bg-blue-500 hover:bg-blue-600">En savoir +</a></div>
                {% if app.user %}
                    {% if "ROLE_STUDENT" in app.user.roles %}
                        {% set find = false %}
                        {% for candidature in offer.candidacies %}
                            {% if candidature.student.user.id == app.user.id %}
                                {% set find = true %}
                            {% endif %}
                        {% endfor %}
                        {% if find %}
                            <div><a href="{{ path('app_delete_candidacy', { 'id': offer.id }) }}"
                                    class="rounded-md p-2 text-white bg-red-500 hover:bg-red-600">Désister</a></div>
                        {% else %}
                            <div><a href="{{ path('app_new_candidacy', { 'id': offer.id }) }}"
                                    class="rounded-md p-2 text-white bg-sky-500 hover:bg-sky-600">Postuler</a>
                            </div>
                        {% endif %}
                    {% elseif "ROLE_ENTERPRISE" in app.user.roles %}
                        {% if offer.enterprise.id == app.user.enterprise.id %}
                            <div><a href="" class="rounded-md p-2 text-white bg-sky-500 hover:bg-sky-600">Modifier</a>
                            </div>
                        {% endif %}
                    {% endif %}
                {% endif %}
            </div>
            <a>test</a>
        </div>

    </div>
{% endblock %}