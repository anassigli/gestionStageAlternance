{% extends 'base.html.twig' %}

{% block body %}
    <div class="grid grid-cols-2 gap-8 mx-auto">
        {% for offer in offers %}
            {% include 'card/card.html.twig' with
                {
                    'offer': offer,
                } %}
        {% endfor %}
    </div>
{% endblock %}