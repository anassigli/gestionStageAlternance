{% extends 'base.html.twig' %}

{% block title "Vos candidatures - ARSA" %}

{% block body %}
<div class="w-3/4 mx-auto">

    <div class="grid grid-cols-2 gap-8 mx-auto">
        {% for candidacy in candidacies %}
            {% set offer = candidacy.offer %}
            {%  include 'card/card.html.twig' with { offer } %}
        {% endfor %}
    </div>

</div>
{% endblock %}
